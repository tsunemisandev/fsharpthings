open System
open 銀行口座

// ❶ 口座を作成し、複数の取引を追加
let 誰かの口座 =
    10000M |> 円
    |> 口座を作成
    |> 入金する (5000M |> 円)
    |> 出金する (2000M |> 円)
    |> 出金する (1000M |> 円)

// ❷ 過去30日間の取引を取得して表示
printfn "\n📜 過去30日間の取引:"
誰かの口座
|> 過去30日間の取引をフォーマット
|> List.iter (printfn "%s")

// ❸ コンソール終了防止
printfn "\n処理が完了しました。Enterキーを押して終了します..."
Console.ReadLine() |> ignore
